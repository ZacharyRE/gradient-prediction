> 本文件保留较早的调度历史，不是当前进程清单。当前主计划已冻结并在GPU1/3运行，用户授权后补充任务使用GPU2，最多同时三张；CPU额外收集器现为exec79598。实时入口见[STATUS.md](STATUS.md)、`scripts/status_final.py`和[WORK_STATE.md](WORK_STATE.md)。后续时间/资源修订不会覆盖下文的历史记录。

# 当前资源调度（02:03 UTC，研究进行中）

硬截止2026-09-10 15:16:40 UTC；探索队列06:30 UTC复审，留出五seed复验、最终评测和报告时间。只用GPU1、GPU3，不干预其他用户进程。

| GPU | 当前任务 | 唯一管理入口 |
| --- | --- | --- |
| 1 | 当前学生目标DFT，随后六个既有严格同题生成来源对照，之后自然停止探索队列 | exec74898 teacher7_expansion_pipeline.py；已结束生成，现训练日志training_after_teacher7_expansion.log |
| 3 | 六个7B扩容开发输出已齐，1500六模型及重启复放完成，恢复开发固定端点评测 | 交接入口exec24946 coverage1500_pipeline.py；已自然接替10529(退出0)，日志evaluation_coverage1500.log |

两个GPU的生成/专项评测都已正常结束并经过两次本研究进程空闲核对，恢复原训练/评测入口。最近baseline和非零adapter开发全文、正确性、停止原因、token数逐项复放全部通过，见teacher1500_restart_protocol_replay.json。当前两个STOP哨兵均不存在；新CPU95659将在最后同题来源训练启动后设置STOP_TRAINER，让该训练完整完成再退出队列；不应启动竞争worker或手动清除哨兵。

队列90项，59项训练已完成；7B扩容三组及其固定平均都已完成。开发平均正确数309/296/290，未平均epoch4为300/289/280，依次对应扩容教师/重复旧题/扩容raw参考。新增7B扩容发布4145题：旧核心1542、新题2603（其中baseline原错609）；固定32条审查剔除5个核心推导错误、1个中途错误否定但最终核验正确的轨迹、2个较轻描述/记号错误。7B扩容、重复、同题raw新目标三个seed43 LR5e-5实验在学生主配对之后执行，固定epoch4；对应数据与参数已发布，交集数据未排训练。学生固定epoch2、32B固定epoch4，不按开发最高epoch选端点。

外部OpenMath12000弱评分和32+24条审查全部完成，因仍漏过实质性错误，保持隔离、没有外部训练任务。各CPU watcher12035/68223/85224/33745均已完成。CPU22730五seed1500严格分析已完成；CPU76659验证样本精度分析已完成。CPU89155已完成三组固定平均。后续五个教师对照为QKVO64两LR、DFT、DFT+KL1和新增DFT+KL0.1；0.1在任何本组DFT训练/分数前追加，使CE/DFT与KL0/0.1/1可比较。CPU71367收集原六组固定比较，CPU34644收集目标函数×KL六格及严格评分。资源监视exec84818与deadline guard exec30636持续运行。

7B平均模型五seed在复用1500题全部超过baseline，平均+2.12pp、两种95%区间为正；开发500题仍有seed47退步。此为候选信号，不是最终成功。最终主配方尚未选择，confirmation_plan.json不存在；OOD878及本轮完整MATH5000均未评测。五seed43–47权重、配置、数据与对照须先冻结；要求每个seed的MATH与数学OOD宏平均都高于baseline，且题目与seed/题目两种均值区间均为正。MATH1500/5000均为历史复用，仅保留数学OOD是本次未触及确认。

调度中已解决的PATH启动与短时自身GPU重叠事故见WORK_STATE.md，有逐文本/逐张量等价复核，不作为原SFT失败原因。自然排空当前任务再接续最终工作，不盲目耗尽24小时。

下一关键节点：六个7B扩容模型的复用1500验证，加上7B rank64、DFT/DFT+KL、学生多文本/重复及四个学生/32B既有DFT对照完成后，决定追加seed或提前冻结已有五seed候选。06:30为最迟复审时间，不要求等到该时刻。GPU3 helper24946已等齐六个开发固定输出并冻结1500模型身份，已完成自然交接，六个1500模型完成且重启复放通过，已恢复开发队列；CPU51393负责统计及重启复放。

最终GPU1原生500题六模型和GPU3冻结基准评测、CPU收集脚本已准备但未启动；最终计划尚未冻结。新增3500题敏感性仅剔除本次已用1500验证题，仍是历史复用，不能称独立测试。

新增CPU88908收集学生/32B同题DFT相对CE，四任务仅调整优先级；没有新增训练组合。当前CPU followup/grid收集入口改为71015/23622，原71367/34644因过严版本守卫已安全重启；版本桥接审计核对两份存档源码只差调度，1000完整输出复放相同。最终冻结将引用当前baseline复放manifest并保留明确版本来源。

在原四个来源DFT之后，提前执行六个既有严格同题来源训练（1465题32B/7B；1429题学生/7B的两个LR），固定epoch4，以消除全部可用训练集不同造成的覆盖混淆。原暂停watcher24298已在触发前安全停掉，新95659等待teacher_common_lr5e5_s43启动后自然停队列；新CPU71120收集三组同题对照。此后决定追加seed或冻结，预计约02:50–03:00UTC。

## 02:32 UTC 最新有界执行安排

以`audits/final_selection_rule.json`和`audits/teacher7_expansion_seed_replication_plan.json`为准。完成六个严格同题来源控制后，旧训练队列通过STOP自然退出；仅补四个扩容7B独立seed44–47。每项训练结束派生固定epoch1..4有效更新平均，GPU3优先评测其开发端点。新增训练最迟06:00 UTC开始、07:00 UTC结束，预期约04:00 UTC完成。

CPU campaign在全部固定开发证据就绪后按前置规则选择主候选，保留三个完整五seed家族，共15adapter+baseline；不会根据最终MATH/OOD再改选。冻结之后，GPU3执行完整5000题和878题OOD，GPU1仅执行所选主候选五seed与baseline的HF/PEFT原生500题。CPU做严格评分、逐题重放及统计汇总。最多两GPU的限制不变；全部计算在15:16:40 UTC前停止并留出报告时间。

当前未来交接由`expanded_seed_replication_pipeline.py`及`final_campaign_pipeline.py`负责；不要同时手动启动另一套训练或最终评测任务。`STOP_TRAINER`在扩容重复与最终原生阶段保持存在，低优先级队列不会恢复。排队94项不等于执行94项，最终清单区分complete、pending、派生adapter、重放和共享历史的续训。
